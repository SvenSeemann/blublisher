<% @notices.each do |n| %>
  <%= link_to n do %>
    <div>
      <h4><%= "#{n.title}" unless n.title.blank? %></h4>
      <h4>
        <%= "Stellenangebot von  #{n.business_name}" if n.title.blank? %>
      </h4>
      <strong><%= "#{n.business_name}" %></strong>
    </div>
  <% end %>
<% end %>