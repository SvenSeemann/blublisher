<% employment_notices.each do |en| %>
  <input type="checkbox" name="employment_notice[]" value="<%= en.id %>" />
  <% if en.title.blank? %>
    <%= "Stellenangebot von #{en.business_name}" %>
  <% else %>
    <%= en.title %>
  <% end %>
  <br />
<% end %>