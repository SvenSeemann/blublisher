<h1>Fachbereich erstellen</h1>

<%= form_for(@department) do |d| %>
  <div class="field">
    <%= d.label :name %>
    <%= d.text_field :name %>
  </div>
  <div class="actions">
    <%= d.submit %>
  </div>
<% end %>
