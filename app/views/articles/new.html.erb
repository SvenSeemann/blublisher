<%= form_for @article do |a| %>
  Titel: <%= a.text_field :title %> <br />
  Inhalt: <%= a.cktext_area :body %>
  <br />
  Kategorie:
  <%= a.collection_select(:category_id, Category.all, :id, :name) %>

  <%= a.submit %>
<% end %>